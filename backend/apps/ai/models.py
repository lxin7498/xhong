# DEPRECATED: This Django app has been replaced by the standalone FastAPI agent (my_agent/).
# The AI chat functionality is served by FastAPI at port 8100 using LangGraph + SSE streaming.
# This app is kept as an empty placeholder and registered in INSTALLED_APPS only to avoid migration noise.
# Feel free to remove this app and the `apps.ai` entry in settings.py when convenient.
